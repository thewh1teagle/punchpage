{
  "name": "punchpage-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.sw.json"
  },
  "dependencies": {
    "nostr-tools": "2.24.1"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "8.2.0"
  }
}
